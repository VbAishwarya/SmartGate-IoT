[pytest]
testpaths = tests
pythonpath = . src
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    vision: tests that require images/videos or OCR/ML dependencies (run with -m vision)
