"""Raspberry Pi hardware entry points (camera, GPIO, servo, etc.).

This package contains code that should run on a Raspberry Pi with real hardware
attached. It is intentionally separate from the core src/ modules so tests and
simulations can run without GPIO/Picamera2 dependencies.
"""

